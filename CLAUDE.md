# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

เกม "สายใยตระกูล" — เกมจำลองตระกูลข้ามรุ่นบนเว็บ ธีมนครอนาคตที่พลังยุทธ์สืบทอดทางสายเลือด
โค้ดและคอมเมนต์ทั้งหมดเป็นภาษาไทย ให้เขียนคอมเมนต์ใหม่เป็นภาษาไทยตามแนวเดิม

## คำสั่งที่ใช้บ่อย

ไม่มี build system ไม่มี package.json ไม่มี dependency — เป็น static site ล้วน

```bash
# รันเกม: เปิดไฟล์ตรงๆ ได้เลย ไม่ต้องมีเซิร์ฟเวอร์
start index.html

# ตรวจ syntax ทุกโมดูล (ทำก่อน commit เสมอ)
for f in js/*.js; do node --check "$f" || echo "FAIL $f"; done

# ตรวจว่า element id ที่โค้ดเรียกใช้มีอยู่ใน index.html ครบ
# (พลาดตรงนี้บ่อยเพราะ UI ถูกสร้างด้วย innerHTML ไม่มีอะไรเตือนตอน build)
grep -oh "getElementById('[^']*')" js/*.js | sort -u
grep -o 'id="[^"]*"' index.html | sort -u
```

### การทดสอบ

ไม่มี test framework ติดตั้งไว้ และไม่มีตัวรัน DOM (ไม่มี jsdom/puppeteer ในเครื่อง)
วิธีที่ใช้ได้จริงคือเขียนสคริปต์ชั่วคราวใน scratchpad แล้วโหลดโมดูลด้วย `vm` ของ Node:

```js
const vm = require('vm');
const root = {};
const sandbox = { self: root, window: root, console, Math, JSON, Date };
sandbox.globalThis = sandbox;
vm.createContext(sandbox);
// ต้องโหลด data.js ก่อนเสมอ ดูหัวข้อ "ลำดับโหลด" ด้านล่าง
for (const f of ['data.js', 'scenario-engine.js', 'person.js', 'lineage.js', 'state.js']) {
  vm.runInContext(fs.readFileSync(`js/${f}`, 'utf8'), sandbox, { filename: f });
}
```

ทำแบบนี้ได้เพราะโมดูลฝั่งตรรกะทั้งหมด **ไม่แตะ DOM เลย** จึงจำลองเกมทั้งเกมใน Node ได้
รูปแบบที่คุ้มที่สุดคือรันจำลอง 40 รอบ รอบละ 100 ปี แล้วดูการกระจายตัว ไม่ใช่รอบเดียว
เพราะบั๊กสมดุลจะโผล่เฉพาะที่หางของการแจกแจง (ดูหัวข้อ "กับดักที่เคยเจอ")

ส่วนเรขาคณิตของเส้นเชื่อมผังตระกูลถูกแยกเป็นฟังก์ชันบริสุทธิ์ `TreeUI.linkSegments(parent, kids)`
โดยเจตนา เพื่อให้ทดสอบได้โดยไม่ต้องมีเบราว์เซอร์ — ถ้าแก้เรื่องเส้น ให้ทดสอบผ่านฟังก์ชันนี้

## สถาปัตยกรรม

### ลำดับโหลดเป็นข้อบังคับ ไม่ใช่แค่ความเรียบร้อย

โมดูล 6 ตัว (`person`, `lineage`, `clock`, `state`, `ui`, `tree-ui`) destructure ค่าจาก
`root.GameData` **ตอนโหลดไฟล์** ไม่ใช่ตอนเรียกใช้ ดังนั้น `data.js` ต้องมาก่อนเสมอ
สลับลำดับ `<script>` ใน `index.html` แล้วเกมจะพังทันทีด้วย TypeError ที่อ่านไม่รู้เรื่อง

ลำดับปัจจุบัน: `data → scenario-engine → person → lineage → clock → state → viewport → ui → tree-ui → main`

ใช้สคริปต์แบบ global (UMD/IIFE) ไม่ใช่ ES modules โดยตั้งใจ เพื่อให้ดับเบิลคลิกเปิด
`index.html` จากไฟล์ตรงๆ ได้โดยไม่ติด CORS — อย่าเปลี่ยนเป็น `import`/`export`

### เส้นแบ่งระหว่างตรรกะกับการแสดงผล

| ไม่แตะ DOM (รันใน Node ได้) | แตะ DOM |
|---|---|
| `data.js` `scenario-engine.js` `person.js` `lineage.js` `clock.js` `state.js` | `ui.js` `tree-ui.js` `viewport.js` `main.js` |

เส้นนี้คือสิ่งที่ทำให้ทดสอบสมดุลเกมได้ **ห้ามให้โมดูลฝั่งซ้ายเรียก `document` เด็ดขาด**
ถ้าต้องการให้ตรรกะสื่อสารกับหน้าจอ ให้คืนค่าออกมาแล้วให้ `main.js` เป็นคนสั่งวาด

### `scenario-engine.js` เป็นโค้ดต้นฉบับของผู้ใช้ — อย่าแก้

เป็นตัวสร้างสถานการณ์แบบสุ่มจาก word banks ที่ผู้ใช้เขียนมาเอง ให้ถือเป็น vendored code
การเปลี่ยนธีมหรือเพิ่มเนื้อหาทำผ่าน public API เท่านั้น โดย `main.js` จะล้างคลังเดิม
แล้วยัดเนื้อหาจาก `GameData.ENGINE_CONTENT` เข้าไปตอนเริ่ม:

```js
engine.locations = []; engine.activities = [];
engine.wordbanks.opening = []; /* ... */
ENGINE_CONTENT.locations.forEach((l) => engine.addLocation(l));
engine.addWordBank('opening', ENGINE_CONTENT.opening);
```

อยากเปลี่ยนธีมทั้งเกม (ชื่อ สถานที่ กิจกรรม เหตุการณ์) แก้ `data.js` ไฟล์เดียวจบ

### วงจรเกม

`clock.js` ยิง tick ทุก `baseTickMs / speed` มิลลิวินาที หนึ่ง tick = หนึ่งเดือนในเกม
`main.js` `tick()` ทำตามลำดับ: เดินปฏิทินและเก็บรายได้ → เพิ่มอายุ (พร้อมคำนวณพลังยุทธ์
กับรายได้ใหม่) → สุ่มหาคู่ → สุ่มมีบุตร → สุ่มเสียชีวิต → เหตุการณ์บรรยากาศ →
สถานการณ์ที่ต้องตัดสินใจ → ตรวจเงื่อนไขชนะ → วาดผล → `pumpDecisions()`

ใช้ `setTimeout` ตั้งใหม่ทุกรอบแทน `setInterval` เพื่อให้เปลี่ยนความเร็วกลางคันมีผลทันที
และไม่มี tick ค้างสะสมตอนสลับแท็บ

### คิวการตัดสินใจ — ผู้เล่นคุมทั้งตระกูล

ไม่มีตัวละครเอก สมาชิกสายเลือด **ทุกคน** ที่มีเรื่องต้องตัดสินใจจะหยุดเวลาแล้วถามผู้เล่น
(`isFounder` เป็นแค่ป้ายอ้างอิงตำแหน่งในผัง ไม่มีสิทธิพิเศษ)

เพราะหลายเรื่องเกิดพร้อมกันได้ใน tick เดียว จึงต้องเข้าคิวแล้วถามทีละเรื่อง กติกาสำคัญ:

- `ask(cfg, onChoose)` ต้องมี `cfg.autoValue` เสมอ — ใช้เมื่ออยู่ในโหมดอัตโนมัติหรือคิวท่วม
- `cfg.subjectId` กันไม่ให้คนเดิมถูกถามเรื่องเดิมซ้ำในเดือนถัดไประหว่างยังค้างคิว
  ต้องลบออกจาก `pendingSubjects` ทุกทางออก ไม่งั้นคนนั้นจะไม่มีวันถูกถามอีก
- คิวเกิน `MAX_QUEUE` (10) จะตัดสินใจอัตโนมัติ กันคิวท่วมตอนเร่งความเร็ว ×16

### เส้นเชื่อมผังตระกูลวาดด้วย SVG ไม่ใช่ CSS

`tree-ui.js` วัดตำแหน่งการ์ดจริงด้วย `getBoundingClientRect` หลังเรนเดอร์ ถอด transform
ออก (หารด้วย `hostRect.width / host.offsetWidth`) แล้ววาดเป็น `<path>` เดียว

**อย่ากลับไปใช้ CSS pseudo-element** วิธีนั้นต้องประกอบเส้นนอนจากชิ้นครึ่งซ้าย/ครึ่งขวา
ของลูกแต่ละคนมาต่อกัน พอผังถูกย่อด้วย `scale()` เป็นทศนิยม รอยต่อจะตกบนพิกเซลไม่ลงตัว
แล้วเผยเป็นรอยขาด — เคยลองแก้เรขาคณิตแล้วยังขาดอยู่ดี เพราะเป็นข้อจำกัดของวิธีเอง

พิกัดที่วาดอยู่ในระบบพิกัดของผังเอง (ก่อน transform) เส้นจึงย่อขยายไปพร้อมการ์ดตอนซูม
โดยไม่ต้องวาดใหม่ ต้องวาดใหม่เฉพาะเมื่อ **layout ของผังเปลี่ยน** เท่านั้น

### ธีมและสีทั้งหมดอยู่ที่ `:root`

`css/style.css` นิยามโทเคนสีสี่ชุด: `:root` (สว่าง), `@media (prefers-color-scheme: dark)`,
`:root[data-theme="dark"]`, `:root[data-theme="light"]` — สองอันหลังจำเป็นเพราะปุ่มสลับธีม
ของผู้ชมต้องชนะ media query ได้ทั้งสองทิศทาง **เพิ่มโทเคนใหม่ต้องเพิ่มครบทั้งสี่ที่**

`--connector` แยกจาก `--line` โดยเจตนา เส้นเชื่อมต้องเข้มกว่าเส้นขอบทั่วไปถึงจะมองเห็น

## กับดักที่เคยเจอ (แก้ไปแล้ว อย่าทำซ้ำ)

**สมดุลประชากรผูกกันสามค่า** `birthChancePerMonth` × `maxChildren` × `marriageRate`
ต้องปรับพร้อมกันเสมอ เคยพังมาแล้วสองทาง: ตั้งสูงไปประชากรระเบิดเป็น 1,685 คนใน 100 ปี
เพราะทุกคู่มีลูกชนเพดานทุกคู่ ผังอ่านไม่ออก — พอลดแรงไปกลับสูญสิ้นสายเลือดตั้งแต่ปีที่ 27
เพราะคู่แรกไม่มีลูกเลย ค่าที่ไวที่สุดคือ `marriageRate` ถ้าตั้ง 1.0 จำนวน "คู่" จะทวีคูณ
ทุกรุ่นเพราะลูกแต่ละคนพาคู่ครองจากนอกตระกูลเข้ามาอีกคน แก้แล้วต้องรันจำลองหลายรอบยืนยัน

**ผู้เล่นเริ่มด้วยพี่น้อง 1–2 คน** ไม่ใช่คนเดียว เพื่อไม่ให้ชะตาทั้งตระกูลแขวนอยู่กับ
การสุ่มมีบุตรของคนเดียว อย่าเอาออก

**`#treePanel` ต้องมี `min-height`** เนื้อหาข้างในเป็น `position: absolute` ทั้งหมด
แถวใน grid จึงไม่มีความสูงในตัวเองและยุบจนผังแทบมองไม่เห็น ตามด้วย `fit()` ที่ย่อผัง
เหลือ 43% จนอ่านไม่ออก

**`fit()` มีเพดานการย่อ `MIN_FIT_SCALE`** ยอมให้เห็นผังไม่ครบแล้วลากดู ดีกว่าเห็นครบ
แต่ตัวหนังสือเล็กจนอ่านไม่ออกเมื่อตระกูลใหญ่ขึ้น

**อย่าจัดมุมมองใหม่ทุกครั้งที่ `resize`** แถบ URL ของเบราว์เซอร์มือถือที่ซ่อน/โผล่
ทำให้เกิด resize รัวๆ ถ้า `fit()` ทุกครั้งจะรีเซ็ตการซูมที่ผู้เล่นเพิ่งตั้งทิ้งกลางคัน
ใช้ `viewport.isUserAdjusted()` เช็คก่อน

**การ์ดห้ามขยับตำแหน่งตอน hover** เส้นเชื่อมวาดจากพิกัดที่วัดไว้แล้ว ถ้าการ์ดเลื่อน
เส้นจะดูหลุดทันที ใช้เปลี่ยนสีขอบแทน `transform: translateY()`

**เส้นนอนต้องคลุมตำแหน่งพ่อแม่ด้วย** ไม่ใช่แค่ช่วง x ของลูก เพราะลูกคนเดียวที่แต่งงานแล้ว
จะมีกล่องคู่กว้างขึ้นจนจุดกึ่งกลางเยื้องออกไป ถ้าคลุมแค่ช่วงลูกแล้วข้ามเส้นนอน
(เพราะคิดว่ามีลูกคนเดียว) เส้นตั้งสองท่อนจะอยู่คนละแกนและลอยห่างกัน

**`refreshFigures()` ต้องวาดเส้นใหม่เมื่อมีใครอายุครบ `adultAge`** เพราะข้อความบนการ์ด
เปลี่ยนจากบรรทัดเดียวเป็นสองบรรทัด ความสูงการ์ดเปลี่ยน เส้นที่วัดไว้จะค้างที่เดิม

## การนำขึ้นใช้งาน

Vercel deploy อัตโนมัติจาก branch `main` — static site, Framework Preset เป็น `Other`,
ไม่มี build command และไม่มี output directory ไฟล์ทั้งหมดอ้างอิงกันด้วย relative path
ตัวพิมพ์เล็กล้วน (Windows ไม่แคร์ตัวพิมพ์แต่เซิร์ฟเวอร์ Linux ของ Vercel แคร์)

git remote ตั้งเป็น `https://maxpuchit8-pixel@github.com/...` โดยเจตนา (ฝังชื่อบัญชีใน URL)
เพราะเครื่องนี้มี credential ของอีกบัญชีค้างอยู่ ถ้าถอดชื่อบัญชีออกจะได้ 403 ตอน push
